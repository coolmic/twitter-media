{% extends 'base.html.twig' %}

{% block body %}
  <div class="container">
    <h2>Twitter media</h2>

    {{ form_start(userForm) }}

    <turbo-frame id="userForm">
      {{ form_widget(userForm) }}
    </turbo-frame>
    <div>
      <button class="btn btn-primary">Submit</button>
    </div>

    {{ form_end(userForm) }}
  </div>
{% endblock %}
